# とるけん（都営ルート優先検索アプリ）

都営交通無料乗車券をお持ちの方のための経路検索PWA。
NPO法人新・障害者支援機構RayV提供。公開URL: https://nporayv.github.io/to-ma_toei/

---

## セッション開始時に必ず読むもの（この順に、全部）

**1つでも飛ばすな。1つだけ読んで「他に作業はない」と報告するな。**
過去に `session-handoff.md` だけ読んで報告し、他の資料にあった保留事項を見落とした。

| # | 読むもの | 内容 | 公開 |
|---|---|---|---|
| 1 | `docs/session-handoff.md` | 現在の状態・進行中タスク・既知の課題・技術詳細 | 非公開(gitignore) |
| 2 | Claudeのメモリ `project_toei_muryo_route` | 判断の根拠・ユーザーの意図・恒久ルール | 非公開・全セッション共有 |
| 3 | Claudeのメモリ `feedback_handoff_local_copy` | 過去のデータ損失事故と、二度と繰り返さないためのルール | 同上 |
| 4 | `.claude/workflow-notes.md` | このデバイス固有の作業ルーティン | 非公開(gitignore) |

### 読んだ後、必ず実測で突き合わせる

書いてある状態と実際が食い違うことがある（コミットが進んでいる等）。**文書を信じて報告しない。**

```bash
git log --oneline -5        # 文書の「現在の状態」と一致するか
git status --porcelain -uall  # 未追跡ファイルの有無（空なら無し）
git check-ignore -v docs/session-handoff.md .claude/workflow-notes.md  # 非公開が効いているか
```

食い違いを見つけたら、**その場で文書とメモリの両方を訂正する**。「後で直す」で放置しない。
放置された誤記録は毎セッション復活し、ユーザーに同じ説明を何度もさせる（実際に起きた）。

### 4か所の重複は冗長ではない。多重防護である

**「メモリに一本化する」「重複しているから片方を消す」は禁止。**
2026-09-01にこの判断で `session-handoff.md` を削除し、約200Kのセッション内容が失われた。
git履歴から復元できたのは偶然にすぎない。一方が壊れても他方から復元するための設計であり、
片方を消した瞬間に防護が消える。

会話が消えた／前回の内容が分からない場合は、下記「セッション消失時」を参照。

---

## アプリの趣旨（最重要）

**都民の近距離移動専用。** 想定は「中野→秋葉原」「新宿→舞浜」程度。

- 県境をまたぐ遠方（秩父・銚子・江ノ島など）への経路や、その精度向上は**対象外**
- 都バスは鉄道でないため対象外
- **運賃の1円単位の精度は目標にしない。**「ジョルダンやGoogleと精度を競うものではない」
- 利用者に障害のある方を想定 → 文字は大きめ、タップ領域44px以上、色だけに頼らない

**スコープ膨張を強く警戒すること。** 達成条件は
(1) より安い経路を見落とさない (2) 案内した額が実際より安くならない
(3) それを `tools/test_fares.js` で機械的に判定できる。
**テストが通ったら手を広げない。**

---

## 絶対に守ること

### 1. このリポジトリは公開されている
- 絶対パス（`C:\Users\<名前>\...`）を、pushされるファイルに書かない
- 作業ログ・判断メモ・引き継ぎ文書を push しない（gitignoreに入れる）
- GitHub Pages経由で `docs/` や `tools/` の中身も誰でも読める

### 2. ファイルを削除・上書きする前に、退避先が実在することを確認する
2026-09-01に `docs/session-handoff.md` を「メモリに一本化する」として削除したが、
移植されたのは要望1件のみで、技術詳細・既知の課題・実装履歴は移らなかった。
git履歴から復元できたが偶然に助けられた形。

**手順**: 移植を先に実行 → 移植先の中身を元ファイルと突き合わせて確認 → その後に削除。
「あとで移す」で先に消さない。

### 3. 障害者割引は事業者ごとに全く違う
都内はすべて100km未満という前提で:
- **京成のみ** 自社線内完結なら距離条件なしで5割引
- 東京メトロ / JR / 東武 / 小田急 / 京急 / 京王 → 100km超のみ = **都内では割引なし**
- 東急 → 単独乗車の割引そのものが無い
- 第1種＋介護者同伴はどの社も距離条件なしで5割引。第2種の介護者は対象外

根拠は `fares.js` の `DISCOUNT_RULES` コメント。**変更時は各社の一次情報で裏を取ること。**

### 4. 見た目を変えたら `sw.js` の `VERSION` を上げる
上げないと利用者の端末でキャッシュが入れ替わらず、変更が反映されない。
`python tools/build_network.py` を実行すると自動更新される。

---

## 開発コマンド

```bash
# ローカル確認
python -m http.server 8788        # → http://localhost:8788
# 反映されないとき: Ctrl+Shift+R → だめなら F12 → Application → Service Workers → Unregister

# 運賃回帰テスト（実測30件。安く出たら不合格）
node tools/test_fares.js

# 駅データ再生成（sw.js の VERSION も自動更新）
python tools/build_network.py
```

---

## ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` / `style.css` / `app.js` | 画面・スタイル・UIロジック |
| `router.js` | 経路探索エンジン |
| `fares.js` | 運賃表・割引ルール |
| `mshiki.js` | イースターエッグ（本体から分離） |
| `sw.js` | Service Worker（`VERSION` でキャッシュ管理） |
| `network.json` | 駅・路線データ（`tools/build_network.py` の生成物） |
| `rayv_toruken_kiyaku_policy.html` | 利用規約・プライバシーポリシー |
| `tools/test_fares.js` | 運賃回帰テスト |

---

## セッション消失時（`/clear`・クラッシュ・誤操作）

**会話記録はディスクに残っており、復元できる。**

```bash
cd ~/.claude/recovery
python recover_session.py list          # セッション一覧
python recover_session.py search "キーワード"   # 全記録から検索
python recover_session.py show <ID先頭6文字>    # 会話をMarkdownに復元
```

詳細な手順は `~/.claude/recovery/RECOVERY.md`。
保持期間は730日に設定済み（既定は30日）。

削除されたファイルは git 履歴からも復元できる:
```bash
git log --all --oneline -- <ファイルパス>   # 削除前のコミットを探す
git show <commit>:<ファイルパス>            # 中身を取り出す
```

---

## ライセンス・出典

- Apache-2.0 / Copyright 2026 NPO法人新・障害者支援機構RayV
- 駅データ出典: [駅データ.jp](https://ekidata.jp/)（加工・再配布可）
